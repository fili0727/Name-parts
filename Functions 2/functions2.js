"use strict";

const firstName = "Filippa";

const animalType = "dog";

const petName = "Bulli";

function sayHello(firstName, animalType, petName) {
  console.log(
    `My name is ${firstName}, i have a ${animalType} called ${petName}`
  );
}

sayHello(firstName, animalType, petName);
